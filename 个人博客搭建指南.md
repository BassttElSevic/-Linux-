# 个人博客搭建完全指南（零基础版）

本文介绍 blog.guaizai.top 和 www.sakimidare.top 这两个个人博客是怎么搭出来的，以及你自己怎么照做。本文假设你对网页相关知识没有任何了解，所有专业名词第一次出现时都会解释。

---

## 第一部分：需要先搞懂的几个基本概念

### 1. 网站到底是什么

一个网站，本质上就是一堆文件（文字、图片、样式）放在一台 24 小时开机的电脑上。别人在浏览器里输入网址，浏览器就把这些文件下载下来，显示成网页。

这台"24 小时开机的电脑"叫作**服务器**，或者叫**主机**。普通人家里没有这种电脑，所以要花钱租，或者用免费的平台。

### 2. 网址（域名）是什么

`blog.guaizai.top` 这种地址叫**域名**。域名是给人记的，因为电脑之间真正通信用的是**IP 地址**（一串数字，比如 104.21.54.30）。

域名需要花钱向**注册商**购买。买了之后，你再告诉全世界"这个域名对应哪台服务器"，这个操作叫**域名解析**（DNS）。做解析的地方可以自己选，一般用免费服务。

### 3. 静态网站和动态网站的区别

- **静态网站**：文件在服务器上是现成的，谁来访问都是同一份内容。个人博客大多属于这种。
- **动态网站**：每次有人访问，服务器现场算一遍再返回内容（比如淘宝、微博）。这种需要租能跑程序的服务器，比较贵。

个人博客用静态网站就够了，因为博客内容更新频率低，而且静态文件加载快。

### 4. 什么是 Markdown

**Markdown** 是一种写文章的简单格式。它用几个符号表示标题、加粗、列表，比如：

```markdown
# 这是大标题
## 这是小标题
**这是加粗文字**
- 这是列表项
```

写博客的人只用写这种纯文本，不需要懂网页代码。写完后由一个程序自动转换成网页格式。

### 5. 什么是"博客框架"

**博客框架**是一个软件，它做的事情是：把你用 Markdown 写的文章，加上你选好的**主题**（主题决定网页长什么样），一次性编译成网页文件。这个编译过程叫**构建**。

两个参考博客用的框架：
- blog.guaizai.top 用 **Hexo**（基于 Node.js）
- www.sakimidare.top 用 **Astro**（基于 Node.js）

### 6. 什么是"部署"和"托管平台"

**部署**就是把编译好的网页文件放到服务器上，让全世界能访问。

**托管平台**就是提供免费服务器的公司。个人博客常用的有三个：
- **Cloudflare Pages**：免费，服务器分布在全球，国内访问相对顺畅
- **Vercel**：免费，国外访问快，国内一般
- **GitHub Pages**：免费，GitHub 自带的托管功能，但国内访问最差。详细介绍见本文第四部分。

前两个平台都可以连接你的 GitHub 仓库：你在本地改完文章、推送到 GitHub，平台自动检测到更新，自动重新编译、自动发布。全程不需要你碰服务器。

### 8. 什么是 HTTPS 和证书

**HTTPS** 是加密的网页传输协议。浏览器地址栏里网址开头是 `https://` 就说明在用加密传输。加密需要**证书**，证书以前要花钱买，现在托管平台自动帮你签发，免费。

---

## 第二部分：这两个博客的完整运作流程

以 sakimidare 的博客为例（guaizai 的流程一模一样，只是换了框架和平台），它从写文章到别人看到，完整走一遍是：

1. 作者在电脑上写一篇 Markdown 格式的文章，存进博客项目的 `posts` 文件夹。
2. 作者执行一个构建命令，Astro 把文章和主题编译成网页文件。
3. 作者用 git 把更新推送到 GitHub 仓库。
4. Vercel 检测到仓库有更新，自动重新构建一遍，把结果发布到全球服务器。
5. 读者在浏览器输入 `www.sakimidare.top`，域名解析告诉他服务器在哪，服务器把网页文件发给他。

作者日常做的事情只有第 1 步（写文章）和第 3 步（推送），其余全部自动化。

具体拆解两个站的配置：

| 项目 | blog.guaizai.top | www.sakimidare.top |
|---|---|---|
| 博客框架 | Hexo（版本 8.1.1） | Astro（版本 5.13.4） |
| 主题 | hexo-theme-fluid | fuwari |
| 源代码存放 | GitHub 公开仓库 | GitHub 公开仓库 |
| 托管平台 | Cloudflare Pages | Vercel |
| 域名 | guaizai.top | sakimidare.top |
| 域名注册商 | NameSilo（美国公司） | 新网（中国公司） |
| 写文章格式 | Markdown | Markdown |

---

## 第三部分：成本清单

| 项目 | 费用 |
|---|---|
| 域名（.top 后缀，首年） | 约 12 到 20 元 |
| 域名（续费，以后每年） | 约 32 到 39 元 |
| 托管平台（Cloudflare Pages 或 Vercel） | 0 元 |
| HTTPS 证书 | 0 元（平台自动签发） |
| 服务器 | 0 元（用平台免费额度） |
| 合计 | 第一年约 12 到 20 元，之后每年 30 到 40 元 |

补充说明：
- 如果完全不想花钱，可以不买域名。Cloudflare Pages 会送你一个免费子域名（形如 `你的名字.pages.dev`），GitHub Pages 也会送你一个（形如 `你的用户名.github.io`），先上线用着，满意了再买域名。
- 域名后缀有很多种，`.top` 是最便宜的之一。`.com` 更正规但贵（一年约 60 到 80 元）。个人博客用 `.top` 完全够。
- 注册商和解析服务是两回事。域名在哪儿买都行，解析可以迁到 Cloudflare 的免费 DNS 上（guaizai 就是这么做的）。

---

## 第四部分：GitHub Pages 托管

### 1. GitHub Pages 是什么

GitHub Pages 是 GitHub 自带的一项免费服务。它把你仓库里的文件直接变成网站：你不用租服务器，不用装任何软件，只要在 GitHub 网站上点几下，GitHub 就把文件发布出去，给你一个免费网址：`你的用户名.github.io`。

它和 Cloudflare Pages、Vercel 是同一类东西，都是"免费托管平台"，区别是它是 GitHub 自己家的，和 GitHub 结合得最紧密。

### 2. 三种用法

| 类型 | 仓库名要求 | 网站地址 |
|---|---|---|
| 用户主页 | 必须是 `你的用户名.github.io` | `你的用户名.github.io` |
| 组织主页 | 必须是 `你的组织名.github.io` | `你的组织名.github.io` |
| 项目主页 | 无要求，任何仓库都行 | `你的用户名.github.io/仓库名` |

个人博客一般用第一种（用户主页），一个账号只能建一个这种站点。

### 3. 怎么开启

1. 打开你的 GitHub 仓库。
2. 点仓库上方的 Settings（设置）。
3. 左侧菜单找 Pages。
4. 在 Source 一栏选分支（比如 main）和目录（根目录或 /docs），点保存。
5. 等一两分钟，GitHub 会显示你的网站地址。

### 4. 两种发布内容的方式

方式一：直接推送构建好的文件
- 本地用 Hexo 等框架把文章编译成网页文件（hexo generate），把编译结果文件夹（public）里的内容推送到仓库。
- GitHub 不做任何处理，仓库里是什么就发布什么。
- 缺点是每次都要手动构建再推送，麻烦。

方式二：用 GitHub Actions 自动构建（推荐）
- GitHub Actions 是 GitHub 自带的自动化功能，可以理解成"在 GitHub 的服务器上替你跑命令"。
- 在仓库里建一个文件夹 `.github/workflows/`，里面放一个配置文件（workflow），写清楚：每次推送代码后，自动执行构建命令，再把结果发布。
- 这样你在本地只要写文章、推送，剩下全自动。
- 网上搜"Hexo GitHub Actions 部署"有现成的配置文件可以抄。

### 5. 它原生支持的框架：Jekyll

GitHub Pages 原生只认识一个博客框架：Jekyll。如果你直接在仓库里放 Markdown 文件，GitHub 会自己用 Jekyll 帮你构建成网站，本地什么都不用装。

但 Jekyll 有缺点：主题老旧、样式朴素，和 Hexo 的 Fluid、Astro 的 fuwari 比颜值差很多。所以大部分人还是用 Hexo 这类框架构建好再传上去。如果你想完全关掉 Jekyll 的处理，在仓库根目录放一个空文件，文件名是 `.nojekyll`。

### 6. 免费额度限制（个人免费账号）

| 项目 | 限制 |
|---|---|
| 发布的内容大小 | 1GB |
| 仓库大小 | 1GB（超过会警告） |
| 带宽 | 每月 100GB |
| 自动构建次数 | 每小时 10 次 |

个人博客文章都是纯文字，几年都到不了这些限制。

### 7. 绑定自定义域名

1. 在你买域名的注册商那里，添加一条 CNAME 解析记录，内容指向 `你的用户名.github.io`。
2. 回到 GitHub 仓库，Settings 里的 Pages 页面，Custom domain 一栏填你的域名，保存。
3. GitHub 会自动签发免费证书，过一会儿就能用你自己的域名访问。

### 8. 三个平台怎么选

| 项目 | GitHub Pages | Cloudflare Pages | Vercel |
|---|---|---|---|
| 费用 | 免费 | 免费 | 免费（条款禁止商业用途） |
| 国内访问 | 最差 | 最好 | 一般 |
| 自动构建 | 只原生支持 Jekyll，其他框架要自己配 Actions | 连接仓库后全自动 | 连接仓库后全自动 |
| 免费证书 | 有 | 有 | 有 |
| 适合场景 | 技术型主页、英文内容、不追求国内访问 | 中文个人博客（最推荐） | 想用现代框架、接受国外速度 |

### 9. 两个参考博客为什么不用 GitHub Pages

guaizai 和 sakimidare 都是中文博客，读者主要在国内，而 github.io 域名在国内经常连不上或者很慢。所以他们都选了 Cloudflare Pages 和 Vercel。如果你不在乎国内访问速度，GitHub Pages 是最省事的方案，因为不用注册新账号。

---

## 第五部分：开始搭建前要注册的账号

1. **GitHub 账号**：存放博客源码。网址 github.com，免费注册。
2. **Cloudflare 账号**（或 Vercel 账号）：托管平台。网址 dash.cloudflare.com 或 vercel.com，免费注册。
3. **域名**：在阿里云（wanwang.aliyun.com）、腾讯云、或 NameSilo 上搜 `.top` 买一个。需要实名认证（中国法律要求，买域名时注册商界面会引导你）。

---

## 第六部分：一步一步搭建（以 Hexo + Cloudflare Pages 为例）

这是目前最省事的组合，适合第一次做网站的人。你的电脑是 Kali Linux，系统已经装好了 Node.js（版本 v24）和 npm（版本 11），下面所有命令直接复制进终端执行即可。

### 步骤 0：安装 Hexo

在终端执行：

```bash
npm install -g hexo-cli
```

`npm` 是 Node.js 自带的软件安装工具，`-g` 表示装到全局（任何目录都能用）。装完执行 `hexo -v` 能显示版本号就成功了。

### 步骤 1：创建博客项目

```bash
cd ~
hexo init myblog
cd myblog
npm install
```

`hexo init myblog` 会在你的主目录下生成一个叫 `myblog` 的文件夹，里面是博客的骨架。`npm install` 是安装博客依赖的组件库。

### 步骤 2：本地预览

```bash
hexo server
```

执行后终端会显示一个地址，一般是 `http://localhost:4000`。在浏览器打开它，你就能在本地看到博客的样子。这是"本地预览"，只有你自己能看，别人访问不了。

按 `Ctrl + C` 可以停止预览。

### 步骤 3：换一个好看的主题

Hexo 默认主题比较朴素。guaizai 用的是 Fluid 主题，安装方式：

```bash
npm install hexo-theme-fluid
```

然后在 `myblog/_config.yml` 文件里（用文本编辑器打开，比如 `nano _config.yml`），找到 `theme:` 这一行，改成：

```yaml
theme: fluid
```

保存后重新执行 `hexo server` 刷新浏览器就能看到效果。

### 步骤 4：写第一篇文章

```bash
hexo new post "我的第一篇文章"
```

这个命令会在 `source/_posts/` 目录下生成一个 Markdown 文件，文件名是"我的第一篇文章.md"。用文本编辑器打开它，把里面的示例内容删掉，写上你自己的内容：

```markdown
---
title: 我的第一篇文章
date: 2026-08-18 20:00:00
---

这是我的博客第一篇文章。

**加粗**文字，*斜体*文字。

## 二级标题

- 列表项一
- 列表项二
```

文章开头的 `---` 之间的内容叫"front matter"，是文章的属性（标题、日期、标签），照格式填就行。

### 步骤 5：把项目推送到 GitHub

1. 登录 GitHub，点右上角新建仓库（New repository），仓库名字随意（比如 `myblog`），创建时**不要**勾选任何初始化选项。
2. 在本地终端执行（把 `你的用户名` 换成你自己的）：

```bash
git init
git add .
git commit -m "我的博客"
git branch -M main
git remote add origin https://github.com/你的用户名/myblog.git
git push -u origin main
```

执行时会让你输入 GitHub 用户名和密码（密码现在指的是"访问令牌"Personal Access Token，GitHub 设置页面里有生成入口，生成后复制粘贴即可）。推送成功后，GitHub 仓库里就能看到全部文件。

### 步骤 6：部署到 Cloudflare Pages

1. 登录 dash.cloudflare.com，注册后进入控制台。
2. 左侧菜单找 Workers 和 Pages，进入 Pages，点"创建项目"。
3. 选择"连接到 Git"，授权 Cloudflare 访问你的 GitHub，然后选 `myblog` 仓库。
4. 构建配置按下面填：
   - 框架预设：Hexo
   - 构建命令：`npm run build`
   - 构建输出目录：`public`
5. 点"保存并部署"。等一两分钟，部署完成后 Cloudflare 会给你一个 `xxx.pages.dev` 的网址，打开它就能看到你的博客，这时全世界都能访问了。

关于构建配置的解释：
- `npm run build` 是让 Hexo 把 Markdown 编译成网页文件的命令。
- `public` 是编译结果存放的文件夹名，平台只把这个文件夹里的内容发布出去。

### 步骤 7：绑定自己的域名

1. 去域名注册商那里，把域名的 DNS 服务器改成 Cloudflare 提供的两个（在 Cloudflare 添加域名后，它会告诉你这两个地址，比如 `sonia.ns.cloudflare.com`）。改完等几小时生效。
2. 在 Cloudflare Pages 项目设置里，"自定义域"一栏添加你的域名，按提示操作。
3. 生效后，访问你的域名就能看到博客。

### 步骤 8：以后每次写文章的流程

以后写新文章只需要四步：

```bash
hexo new post "新文章标题"   # 1. 新建文章文件
# 2. 用编辑器打开刚生成的文件，写内容
hexo generate                 # 3. 本地编译，确认没问题
git add .
git commit -m "新增文章"
git push                      # 4. 推送，Cloudflare 自动部署
```

推送后等一两分钟，刷新你的域名就能看到新文章。不用再操作 Cloudflare。

---

## 第七部分：另一种方案（Astro + Vercel）

sakimidare 的方案，视觉效果更现代，但需要理解一些前端概念，门槛稍高。流程和上面一样，只是工具不同：

1. 创建项目：`npm create fuwari@latest`（这个命令会生成一个 fuwari 主题的 Astro 博客项目，fuwari 是一个现成的博客模板）。
2. 改配置文件 `src/config.ts`（站名、头像、社交链接都在里面）。
3. 写文章：`npm run new-post 文章名`，文章存在 `src/content/posts/`。
4. 推送到 GitHub，在 vercel.com 上导入仓库，Vercel 会自动识别 Astro 项目，构建命令和输出目录它会自动填好。
5. 绑定域名：把 `www` 子域名的解析指向 Vercel 提供的地址（CNAME 记录）。

Astro 方案需要先安装 pnpm 这个包管理工具：`npm install -g pnpm`。

---

## 第八部分：常见问题

### 1. 国内能不能访问？

国内访问从好到差排序：Cloudflare Pages 最好（一般能访问，偶尔不稳定），Vercel 一般，GitHub Pages 最差（github.io 域名在国内经常连不上或者很慢，所以两个参考博客都没有用它）。如果追求稳定，可以后续把域名解析迁回国内 CDN 或备案后使用国内主机（备案需要域名实名认证满一定时间，流程较长）。对个人博客来说，先用 Cloudflare Pages 是最简单的。

### 2. 博客源码公开还是私有？

GitHub 仓库可以是私有的（别人看不到源码），不影响自动部署。guaizai 和 sakimidare 都选择了公开，公开的好处是别人能看你的代码、给你提改进建议。

### 3. 要不要评论功能？

两个参考博客都没有装评论系统。如果要加，常见的免费方案有 Waline、Twikoo、Giscus 等，等博客搭好之后再研究不迟。评论功能不是搭建博客的必需品。

### 4. 要不要统计访问量？

Cloudflare 自带免费的网站统计（Web Analytics），在控制台里开启即可，不需要额外装东西。

### 5. 文章里的图片放哪里？

图片可以放在博客项目的 `source/images/` 文件夹里，和文章一起推送。注意仓库有大小限制，大量图片建议用图床（专门存图片的免费服务）或者对象存储。

### 6. 免费额度会不会不够用？

个人博客流量很小。Cloudflare Pages 免费版带宽不限；Vercel 免费版每月 100GB 带宽，正常写博客远用不完。

### 7. Vercel 免费版的限制？

Vercel 免费版（Hobby 计划）在条款上禁止商业用途，只允许个人非商业项目。纯个人博客没问题，但如果以后想挂广告或卖东西，需要升级付费版。

---

## 附：本文用到的名词速查表

| 名词 | 意思 |
|---|---|
| 服务器 | 24 小时开机的电脑，存放网站文件 |
| 域名 | 网址，如 blog.guaizai.top，花钱买 |
| IP 地址 | 服务器的数字地址，域名解析到它 |
| DNS | 域名解析服务，把域名对应到服务器 |
| Markdown | 写文章用的简单文本格式 |
| 博客框架 | 把 Markdown 文章编译成网页的程序（Hexo、Astro、Hugo） |
| 主题 | 决定网页外观的模板 |
| 构建 | 把源文件编译成网页文件的过程 |
| GitHub Pages | GitHub 自带的免费静态网站托管功能 |
| 推送（push） | 把本地文件上传到 GitHub |
| 托管平台 | 免费提供服务器和自动部署的公司（Cloudflare Pages、Vercel、GitHub Pages） |
| 部署 | 把网页文件放到服务器上让别人能访问 |
| HTTPS | 加密的网页传输协议 |
| 证书 | HTTPS 加密需要的凭证，托管平台免费提供 |
| CDN | 分布在世界各地的服务器缓存，加快访问速度 |
