# OBS 没有"屏幕捕获"选项 —— 完整调试日志与避坑手册

> **场景**：OBS Studio 在 **niri（Wayland 平铺式窗口管理器）** 会话下，来源面板完全没有"屏幕捕获 (PipeWire)"选项，无法录制屏幕。
> **结论先行**：niri 的屏幕共享需要 **`xdg-desktop-portal-gnome`**（不是 kde，也不是 wlr 版本）。装错 portal 包是这类问题最常见的坑。
> **状态**：✅ 已修复，可正常录制。

---

## 1. 环境信息（复现此问题的系统配置）

| 项目 | 值 | 说明 |
|---|---|---|
| 发行版 | Kali GNU/Linux Rolling 2026.3 | 基于 Debian sid |
| 会话类型 | `wayland`（`XDG_SESSION_TYPE=wayland`） | 关键前提 |
| 桌面环境 | **niri**（`XDG_CURRENT_DESKTOP=niri`） | 非 GNOME/KDE，极易踩坑 |
| OBS 安装方式 | **Flatpak**（`com.obsproject.Studio` 32.2.2） | 沙箱应用，走 portal 采集 |
| PipeWire | 1.6.8，正常 `active (running)` | 采集必需，但仅此还不够 |
| 修复前已装 portal | `xdg-desktop-portal`、`-gtk`、`-kde` | **缺 `-gnome`，这是根因** |

---

## 2. 问题现象

- OBS 来源面板点 **+**，只有"视频采集设备""文本"等，**没有"屏幕捕获 (PipeWire)"和"显示器采集"**
- 其他应用（浏览器等）也无法正常共享屏幕（可据此确认是系统级问题，不是 OBS 的事）
- OBS 日志里的**决定性报错**：

```
[pipewire] No capture sources available
```

> ⚠️ 看到这句日志，基本可以断定是 portal 的 ScreenCast 接口不可用，而不是 OBS 本身的问题。

---

## 3. 踩过的弯路（反面教材）

一开始按网上通用教程执行了：

```bash
sudo apt install xdg-desktop-portal xdg-desktop-portal-kde
```

**重启后依然无效。** 原因有二：

1. **KDE portal 后端只在 Plasma 会话下才会被激活**。当前会话是 niri，`xdg-desktop-portal-kde.service` 根本不会启动；
2. KDE portal 即使激活，也**不提供** `ScreenCast`（屏幕共享）接口 —— 屏幕共享是 GNOME portal 的活。

> 💡 **教训**：搜"Linux OBS 屏幕捕获"教程时，教程默认针对 GNOME/KDE，而 niri 这类小众 wlroots 合成器（sway、river、hyprland 同理）需要单独确认官方要求。

---

## 4. 诊断命令（按顺序执行，附输出解读）

以下命令均可在普通终端执行，**不需要 root**（除安装步骤外）。

### 4.1 确认会话类型和桌面环境

```bash
echo "$XDG_SESSION_TYPE $XDG_CURRENT_DESKTOP"
# 输出: wayland niri   ← 关键：niri 不是 KDE！
```

### 4.2 确认 OBS 是 Flatpak 装的（影响日志路径和沙箱行为）

```bash
flatpak list | grep -i obs
# 输出: OBS Studio com.obsproject.Studio 32.2.2 stable system
```

### 4.3 看 OBS 的沙箱环境

```bash
flatpak run --command=env com.obsproject.Studio | grep -E "XDG_SESSION_TYPE|XDG_CURRENT_DESKTOP|WAYLAND_DISPLAY"
# 输出:
# XDG_RUNTIME_DIR=/run/user/1000
# WAYLAND_DISPLAY=wayland-1
# XDG_CURRENT_DESKTOP=niri
# XDG_SESSION_TYPE=wayland
```

### 4.4 检查哪些 portal 后端装了、哪些在运行

```bash
# 已安装的包
dpkg -l | grep -E "xdg-desktop-portal"
# 修复前: portal, portal-gtk, portal-kde  ← 没有 portal-gnome！

# 实际在运行的后端（注意：装了≠在跑）
systemctl --user list-units --all | grep -i portal
# 修复前只有: flatpak-portal / gtk / xdg-desktop-portal / document-portal
#             ← 没有 xdg-desktop-portal-gnome.service！
```

> 🔑 **核心判断**：GTK portal（`xdg-desktop-portal-gtk`）只提供文件选择等基础功能，**不提供屏幕共享**。所以当时系统里没有任何可用的 ScreenCast 实现 → OBS 报 `No capture sources available`。

### 4.5 查 OBS 日志（Flatpak 版日志路径）

```bash
ls -t ~/.var/app/com.obsproject.Studio/config/obs-studio/logs/ | head -3
grep -iE "portal|pipewire|screencast" <最新日志文件>
# 决定性报错: [pipewire] No capture sources available
```

> 📂 Flatpak 版 OBS 的配置/日志都在 `~/.var/app/com.obsproject.Studio/` 下，和 apt 版（`~/.config/obs-studio/`）位置不同，排查时别找错。

### 4.6 验证修复后：ScreenCast 接口已暴露到 D-Bus

```bash
busctl --user introspect org.freedesktop.impl.portal.desktop.gnome \
  /org/freedesktop/portal/desktop | grep -i screencast
# 修复后输出: org.freedesktop.impl.portal.ScreenCast  ← 这就是 OBS 要的接口
```

> 修复前这条命令**查不到任何 ScreenCast 接口**（连 `org.freedesktop.impl.portal.desktop.gnome` 这个服务都不存在）。

---

## 5. 根因分析

niri 官方文档明确写了屏幕共享的前置条件：

> **Screencasting（niri 官方文档）**：
> "The primary screencasting interface that niri offers is through portals and pipewire... you need a working D-Bus session, pipewire, **`xdg-desktop-portal-gnome`**."
> 来源：https://niri-wm.github.io/niri/Screencasting.html

> **Important Software（niri 官方文档）**：
> "`xdg-desktop-portal-gnome`: required for screencasting support"
> 来源：https://niri-wm.github.io/niri/Important-Software.html

**为什么是 gnome portal？** niri 实现了 GNOME 的 RemoteDesktop/ScreenCast 协议（这是 Wayland 生态里屏幕共享的事实标准实现），所以需要 gnome 后端来桥接。

**完整因果链**：
```
装错 portal 包（kde）
→ KDE 后端在 niri 会话不激活
→ 唯一在跑的 gtk 后端不提供 ScreenCast
→ D-Bus 上无 ScreenCast 接口
→ OBS: "[pipewire] No capture sources available"
→ 来源列表里没有"屏幕捕获"选项
```

---

## 6. 修复步骤（三分钟搞定）

```bash
# 1) 装正确的包（Kali/Debian；Arch 系是 xdg-desktop-portal-gnome 同样名称）
sudo apt install xdg-desktop-portal-gnome

# 2) 重启 portal 服务（无需重启电脑）
systemctl --user restart xdg-desktop-portal.service xdg-desktop-portal-gtk.service

# 3) 完全退出 OBS 再打开 → 来源 + 里出现 "屏幕捕获 (PipeWire)" 🎉
```

**可选**：验证服务状态

```bash
systemctl --user list-units | grep portal
# 应看到: xdg-desktop-portal-gnome.service ... active (running)
```

---

## 7. 避坑清单（分享给别人的重点）

### 7.1 装 portal-gnome 会连装一堆 GNOME 组件 —— 正常
依赖链：`xdg-desktop-portal-gnome → gnome-shell → gdm3 / gnome-control-center / gnome-sushi...`
体积不小（几百 MB），但**这是必需依赖**，别去 purge。

### 7.2 安装时出现 `gdm.service is not active, cannot reload` —— 无害，无视
原因：gdm3 被顺带装上，但你的会话是 niri 不是 GDM 登录的，gdm 服务从未运行（`systemctl status gdm` 显示 `inactive (dead)` + `disabled`），安装脚本"重载"一个没在跑的服务自然失败。
以后升级涉及 gnome-shell/gdm3 的包时大概率还会看到，**每次都可以无视**。

### 7.3 首次采集会弹授权框
niri 会弹出窗口选择器（列出你的显示器和各窗口），**必须选目标并点"共享"**，否则画面黑屏。选"窗口"可以只录单个窗口。

### 7.4 如果"屏幕捕获"选项有了，但采集出来是黑屏（备选方案）
niri 有已知 issue（niri-wm/niri#2223）：部分场景下走 portal 的 ScreenCast 会黑屏。OBS 32.x 提供了**不依赖 portal** 的备选采集源：
- 来源 + → **"Wayland output (scpy)"**（基于 wlr-screencopy 协议，niri 下更稳）

### 7.5 别同时装 gnome 和 wlr 两种 portal 再裸奔
niri 已知问题（niri-wm/niri#3798）：同时存在 `xdg-desktop-portal-gnome` 和 `xdg-desktop-portal-wlr` 且没有 `portals.conf` 时，gnome 后端会赢下 D-Bus 仲裁，wlr 后端永不激活。**装 gnome 一个就够了**，不要画蛇添足。

### 7.6 其他系统的"没有屏幕捕获"速查（不是本案例，但常被一起搜到）

| 系统 | 常见原因 | 快速解法 |
|---|---|---|
| Linux + X11 会话 | 一般直接有"显示器采集"，不走 portal | 无需 portal，直接添加 |
| Linux + Wayland + GNOME/KDE | 缺 xdg-desktop-portal | `apt install xdg-desktop-portal-gnome` / `-kde` |
| Linux + Wayland + wlroots 系（niri/sway/hyprland/river） | 需 gnome portal | 本手册第 6 节 |
| Windows | 双显卡冲突 / 硬件加速 GPU 计划 | NVIDIA 面板给 OBS 指定"集成图形"；设置→系统→屏幕→显示卡→默认图形设置→关闭"硬件加速 GPU 计划"后重启 |
| macOS | 缺少屏幕录制权限 | 系统设置→隐私与安全性→屏幕录制→勾选 OBS |

---

## 8. 通用判断流程（下次 5 分钟定位）

```
OBS 没有"屏幕捕获"选项
│
├─ 看 OBS 日志（日志目录见 4.5）
│   └─ 有 "[pipewire] No capture sources available"
│       └─ 定位到 portal 问题 ↓
│
├─ echo $XDG_SESSION_TYPE $XDG_CURRENT_DESKTOP
│   ├─ 不是 wayland → 按 X11 处理（一般直接有选项）
│   └─ 是 wayland →
│       ├─ GNOME/KDE 会话 → 装对应 portal 即可
│       └─ niri 等 wlroots 系 → 装 xdg-desktop-portal-gnome（本手册第 6 节）
│
└─ busctl --user introspect ... | grep ScreenCast
    └─ 查不到 → portal 后端没提供屏幕共享，按上面链路装包
```

---

## 9. 参考链接

- niri 官方文档 · Screencasting：https://niri-wm.github.io/niri/Screencasting.html
- niri 官方文档 · Important Software：https://niri-wm.github.io/niri/Important-Software.html
- niri issue #2223（ScreenCast 黑屏 / scpy 备选）：https://github.com/niri-wm/niri/issues/2223
- niri issue #3798（gnome+wlr 仲裁冲突）：https://github.com/niri-wm/niri/issues/3798
- OBS 官方论坛（Wayland 无屏幕捕获选项讨论）：https://obsproject.com/forum/threads/no-screen-capture-option-wayland.178447/

---

*记录时间：2026-08-19 · 环境：Kali Rolling / niri 26.04 / OBS 32.2.2 (Flatpak) / PipeWire 1.6.8*
*结论一句话：**niri 用户请装 `xdg-desktop-portal-gnome`，装 kde 版本没用。***
